# 🚀 Server Performance Stats

A simple bash script to check CPU, Memory, and Disk usage on your Linux server.

## 💾 Installation & Usage

Follow these four steps to run the script:

1.  **Clone the repository:**
    ```bash
    git clone git@github.com:ElyorXujam/prcServerStats.git
    ```

2.  **Navigate to the folder:**
    ```bash
    cd prcServerStats
    ```

3.  **Make the script executable:**
    ```bash
    chmod +x server-stats.sh
    ```

4.  **Run the script:**
    ```bash
    ./server-stats.sh
    ```
    
This project is part of [roadmap.sh](https://roadmap.sh/projects/server-stats) DevOps projects.